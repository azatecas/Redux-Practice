//setup initialState
const initialState = {
    players: [],
    isFetching: false,
    error: ""
}

//setup an empty reducer
const playerReducer = (state = initialState, action) => {
    switch (action.type) {
        case value:
            
            break;
    
        default:
            return state;
    }
}

export default playerReducer;